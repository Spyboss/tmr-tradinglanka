{
  "name": "gunawardhana-motors",
  "version": "2.0.0",
  "description": "Comprehensive business management system for motorcycle dealerships",
  "private": true,
  "author": "Uminda Herath <contact@uhadev.com> (https://uhadev.com)",
  "license": "MIT",
  "keywords": [
    "business-management",
    "motorcycle-dealership",
    "inventory-management",
    "sales-management",
    "quotation-system",
    "react",
    "nodejs",
    "typescript",
    "mongodb"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/gunawardhana-motors.git"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "check": "npm run check:backend && npm run check:frontend",
    "check:backend": "npm run lint --prefix backend && npx tsc --noEmit --project backend/tsconfig.json && npm run test --prefix backend",
    "check:frontend": "npm run build --prefix frontend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "@modelcontextprotocol/server-memory": "^2025.4.25",
    "@types/axios": "^0.9.36",
    "axios": "^1.11.0",
    "date-fns": "^4.1.0",
    "dayjs": "^1.11.13",
    "ioredis": "^5.6.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
